import 'package:flutter/material.dart';

const primaryColor = Color(0xFAF67308);
const secondaryColor = Color(0xFF272B27);
const darkColor = Color.fromARGB(0, 0, 0, 0);
const bodyTextColor = Color(0xFF828288);
const bgColor = Color(0xDC141414);
const exp = Color(0xFF64F403);

const defaultPadding = 20.0;
const defaultDuration = Duration(seconds: 1);
const maxWidth = 1440.0;

const brd = 100.0;